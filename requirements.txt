# 운영 의존성 — 정확한 버전 핀.
# 매 빌드마다 PyPI 최신 patch 가 자동으로 끌려오는 것을 막아 공급망 위험과
# 회귀를 줄인다. 갱신 시 `.venv/bin/pip freeze` 결과를 그대로 반영하고,
# 핵심 패키지 (anthropic, httpx, apscheduler, holidays) 는 CHANGELOG 검토 후 업.
#
# 추가 가이드:
#   pip install -r requirements.txt  (운영)
#   pip install "httpx[http2]" pyyaml python-dotenv anthropic apscheduler holidays
#       후 `pip freeze > requirements.txt` (갱신)

# 직접 의존성
httpx[http2]==0.28.1
PyYAML==6.0.3
python-dotenv==1.2.1
anthropic==0.93.0
APScheduler==3.11.2
holidays==0.83

# 전이 의존성 (재현성 보장)
annotated-types==0.7.0
anyio==4.12.1
certifi==2026.2.25
distro==1.9.0
docstring_parser==0.17.0
exceptiongroup==1.3.1
h11==0.16.0
h2==4.3.0
hpack==4.1.0
httpcore==1.0.9
hyperframe==6.1.0
idna==3.11
jiter==0.13.0
pydantic==2.12.5
pydantic_core==2.41.5
python-dateutil==2.9.0.post0
six==1.17.0
sniffio==1.3.1
typing_extensions==4.15.0
typing-inspection==0.4.2
tzlocal==5.3.1
